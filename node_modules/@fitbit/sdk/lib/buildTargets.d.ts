import { BuildTarget } from './ProjectConfiguration';
export interface BuildTargetDescriptor {
    displayName: string;
    platform: string[];
    resourceFilterTag: string;
}
declare const buildTargets: {
    [platform in BuildTarget]: BuildTargetDescriptor;
};
export default buildTargets;
