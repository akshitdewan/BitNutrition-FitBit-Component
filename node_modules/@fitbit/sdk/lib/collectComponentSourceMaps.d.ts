/// <reference types="node" />
import stream from 'stream';
import { ComponentType } from './componentTargets';
import { BuildTarget } from './ProjectConfiguration';
export default function collectComponentSourceMaps(): {
    collector: (componentType: ComponentType, componentPlatform?: BuildTarget | undefined) => stream.Transform;
    emitter: stream.PassThrough;
};
